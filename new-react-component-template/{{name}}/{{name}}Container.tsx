import React from "react";
import { {{name}} } from './{{name}}'

export interface {{name}}ContainerProps {}

export const {{name}}Container: React.FC<{{name}}ContainerProps> = (props) => {
  return <{{name}} />;
};
